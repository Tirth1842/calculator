import React, { Component } from 'react';

class ButtonPanel extends Component {
    render () {
        return (
            <div>welcome to the button pannel</div>
        );
    }
}

export default ButtonPanel;